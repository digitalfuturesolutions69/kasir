DATABASE_URL="file:./dev.db"
JWT_SECRET="replace-with-a-long-random-secret"

# Needed for the "unggah foto bukti transaksi" feature (reads amount/type
# from a receipt photo via Claude Vision). Get a key at https://console.anthropic.com/
# Without this, photo upload still saves the image but skips auto-fill —
# users just enter the amount/type manually.
ANTHROPIC_API_KEY=""
